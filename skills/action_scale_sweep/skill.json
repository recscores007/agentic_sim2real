{
  "id": "action_scale_sweep",
  "name": "Action Scale Sweep",
  "owner_agent": "sysid_agent",
  "description": "Generate and validate small action-scale candidates for contact-rich insertion.",
  "implementation": "action_scale_sweep",
  "depends_on": ["sysid_step_response"],
  "inputs": ["gap_estimates", "config"],
  "outputs": ["action_scale_sweep.json", "result.json"],
  "validators": [
    "suggested action scale must be <= configured maximum",
    "contact force over-limit ratio must stay <= 5 percent",
    "candidate list must include nominal action scale"
  ],
  "quality_gate": {"min_score": 0.7},
  "human_required": false,
  "release_blocking": true,
  "real_robot": false
}
