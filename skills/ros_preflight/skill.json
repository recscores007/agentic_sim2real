{
  "id": "ros_preflight",
  "name": "Deployment Middleware Preflight",
  "owner_agent": "orchestrator_agent",
  "description": "Validate the configured deployment middleware contract without commanding robot motion.",
  "implementation": "ros_preflight",
  "depends_on": ["env_preflight"],
  "inputs": ["config"],
  "outputs": ["ros_preflight_contract.json", "result.json"],
  "validators": [
    "workflow_type must match the configured deployment contract",
    "RMW_IMPLEMENTATION should be rmw_cyclonedds_cpp",
    "ROS_DOMAIN_ID must be explicitly configured"
  ],
  "quality_gate": {"min_score": 0.8},
  "human_required": false,
  "release_blocking": true,
  "real_robot": false
}
