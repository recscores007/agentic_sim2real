{
  "id": "policy_artifact_audit",
  "name": "Policy Artifact Audit",
  "owner_agent": "sim_agent",
  "description": "Check that a deployable policy bundle has the expected metadata, agent config, and environment config.",
  "implementation": "policy_artifact_audit",
  "depends_on": ["isaaclab_task_check"],
  "inputs": ["policy_artifact_dir"],
  "outputs": ["policy_artifact_audit.json", "result.json"],
  "validators": [
    "agent.yaml must exist",
    "env.yaml must exist",
    "checkpoint.meta.json must exist and describe the checkpoint used for release"
  ],
  "quality_gate": {"min_score": 0.7},
  "human_required": false,
  "release_blocking": true,
  "real_robot": false
}
