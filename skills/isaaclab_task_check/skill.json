{
  "id": "isaaclab_task_check",
  "name": "Isaac Lab Task Contract Check",
  "owner_agent": "sim_agent",
  "description": "Validate the configured Isaac Lab task id, end-effector choice, observation contract, and nominal action scale.",
  "implementation": "isaaclab_task_check",
  "depends_on": ["env_preflight"],
  "inputs": ["config"],
  "outputs": ["task_contract.json", "result.json"],
  "validators": [
    "task id must match the configured task contract for this example",
    "policy observations must match the configured observation contract",
    "nominal action scale must match the configured end-effector contract"
  ],
  "quality_gate": {"min_score": 0.9},
  "human_required": false,
  "release_blocking": true,
  "real_robot": false
}
