{
  "depends_on": [
    "ros_preflight",
    "real_data_quality_gate"
  ],
  "description": "Validate object pose-estimation repeatability against the configured real-robot pose gate.",
  "human_required": true,
  "id": "pose_repeatability",
  "implementation": "pose_repeatability",
  "inputs": [
    "dataset_jsonl"
  ],
  "name": "Object Pose Repeatability",
  "outputs": [
    "pose_repeatability.json",
    "result.json"
  ],
  "owner_agent": "perception_agent",
  "quality_gate": {
    "min_score": 0.7
  },
  "real_robot": false,
  "release_blocking": true,
  "validators": [
    "position_error_p95_m must be <= 0.01",
    "at least the configured minimum number of pose samples is required",
    "orientation error must be reported when quaternions are present"
  ]
}
