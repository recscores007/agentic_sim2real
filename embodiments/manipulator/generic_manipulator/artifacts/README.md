# Artifacts

Put local checkpoints, exported policy files, calibration outputs, simulator
assets, and generated deployment bundles here. Keep large binaries outside git
or document where to fetch them.
