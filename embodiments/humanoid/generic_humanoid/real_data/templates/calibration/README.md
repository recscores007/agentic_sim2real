# Calibration

File: `calibration.json`

Record robot frames, sensor frames, calibration source files, and validation
metrics. The evaluator can use these values to decide whether perception and
hardware evidence are trustworthy.
