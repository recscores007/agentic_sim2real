# Example Session

This synthetic session mirrors the template layout and can be used to test the
real-data converter:

```bash
./scripts/prepare_real_data.sh embodiments/<embodiment_type>/<embodiment_name>/real_data/example_session
```

Replace these rows with real logs before using the session for release
decisions.
